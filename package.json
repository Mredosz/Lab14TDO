{
  "name": "lab-14-tdo",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "jest --coverage --testPathIgnorePatterns=integration-test.js",
    "integration-test": "jest integration-test.js",
    "dev": "node src/index.js",
    "build": "echo 'Building the app...' && mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "eslint": "^8.57.1",
    "globals": "^16.2.0",
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "prettier": "^2.8.0"
  }
}
